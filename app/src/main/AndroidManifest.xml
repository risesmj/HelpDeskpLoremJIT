<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.loremjit.centraldechamados">

    <application
        android:name=".realm.AplicacaoRealm"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ComentarioChamadoActivity"
            tools:ignore="Instantiatable"></activity>
        <activity android:name=".activities.VisualizacaoChamadoActivity"
            tools:ignore="Instantiatable" />
        <activity android:name=".activities.NovoChamadoActivity"
            tools:ignore="Instantiatable" />
        <activity android:name=".activities.LoginActivity"
            tools:ignore="Instantiatable" />
        <activity android:name=".MainActivity"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>